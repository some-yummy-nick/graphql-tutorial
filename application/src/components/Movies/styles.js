export const styles = theme => ({
    wrapper: {
        position: 'relative',
        minHeight: 'calc(100vh - 24px * 2 - 72px)',
    },
    fabWrapper: {
        paddingTop: theme.spacing(2),
        textAlign: "right"
    },
});
